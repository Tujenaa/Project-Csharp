@page
@model GPSGuide.Web.Pages.IndexModel
@{
    ViewData["Title"] = "Dashboard";
    ViewData["Active"] = "home";
}

<<<<<<< HEAD
@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="alert alert-danger">
        <i class="bi bi-exclamation-triangle-fill"></i>
        @Model.ErrorMessage
    </div>
}

<!-- ── Stat cards ── -->
<div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(200px,1fr));gap:16px;margin-bottom:28px">
    <div class="stat-card">
        <div class="stat-icon" style="background:#dbeafe;color:#1d4ed8">
            <i class="bi bi-geo-alt-fill"></i>
        </div>
        <div class="stat-body">
            <div class="stat-val">@Model.TotalPois</div>
            <div class="stat-label">Điểm thuyết minh</div>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon" style="background:#dcfce7;color:#15803d">
            <i class="bi bi-play-circle-fill"></i>
        </div>
        <div class="stat-body">
            <div class="stat-val">@Model.TotalPlays</div>
            <div class="stat-label">Tổng lượt phát</div>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon" style="background:#fef9c3;color:#a16207">
            <i class="bi bi-people-fill"></i>
        </div>
        <div class="stat-body">
            <div class="stat-val">@Model.TotalUsers</div>
            <div class="stat-label">Người dùng</div>
        </div>
    </div>
    <div class="stat-card">
        <div class="stat-icon" style="background:#fce7f3;color:#be185d">
            <i class="bi bi-clock-history"></i>
        </div>
        <div class="stat-body">
            <div class="stat-val">@Model.TotalHistory</div>
            <div class="stat-label">Lịch sử phát</div>
        </div>
    </div>
</div>

<!-- ── Nội dung 2 cột ── -->
<div style="display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-bottom:20px">

    <!-- Top POI -->
    <div class="card">
        <div class="card-head">
            <i class="bi bi-bar-chart-fill me-2" style="color:#3b82f6"></i>
            Top điểm được nghe nhiều nhất
        </div>
        <table class="tg-table">
            <thead>
                <tr><th>#</th><th>Điểm POI</th><th>Lượt phát</th></tr>
            </thead>
            <tbody>
                @{
                    int rank = 1;
                }
                @foreach (var s in Model.TopPois)
                {
                    <tr>
                        <td class="text-muted">@rank</td>
                        <td style="font-weight:500">@s.Name</td>
                        <td>
                            <span style="background:#dbeafe;color:#1d4ed8;
                                             padding:2px 8px;border-radius:4px;
                                             font-size:12px;font-weight:600">
                                @s.Count
                            </span>
                        </td>
                    </tr>
                    rank++;
                }
                @if (!Model.TopPois.Any())
                {
                    <tr>
                        <td colspan="3" class="empty-state">Chưa có dữ liệu</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Lịch sử gần đây -->
    <div class="card">
        <div class="card-head">
            <i class="bi bi-clock-history me-2" style="color:#8b5cf6"></i>
            Lịch sử phát gần đây
=======
@if (Model.ApiError)
{
    <div class="alert alert-warning">
        <i class="bi bi-exclamation-triangle"></i>
        Không kết nối được API tại <code>@Model.ApiUrl</code> — kiểm tra TourGuideAPI có đang chạy không.
    </div>
}

<div class="stat-grid">
    <div class="stat-card blue">
        <div>
            <div class="stat-value">@Model.TotalPoi</div>
            <div class="stat-label">Điểm thuyết minh</div>
        </div>
        <i class="bi bi-geo-alt stat-icon"></i>
    </div>
    <div class="stat-card cyan">
        <div>
            <div class="stat-value">@Model.TotalAudio</div>
            <div class="stat-label">File Audio / Script</div>
        </div>
        <i class="bi bi-music-note stat-icon"></i>
    </div>
    <div class="stat-card green">
        <div>
            <div class="stat-value">@Model.TotalHistory</div>
            <div class="stat-label">Lượt phát tổng</div>
        </div>
        <i class="bi bi-play-circle stat-icon"></i>
    </div>
    <div class="stat-card yellow">
        <div>
            <div class="stat-value">@Model.TotalUsers</div>
            <div class="stat-label">Người dùng</div>
        </div>
        <i class="bi bi-people stat-icon"></i>
    </div>
</div>

<div style="display:grid; grid-template-columns:1fr 1fr; gap:20px; margin-top:20px">
    <div class="card">
        <div class="card-header">
            <span><i class="bi bi-clock-history me-2"></i>Lịch sử phát gần đây</span>
            <a href="/HistoryPage" class="btn btn-ghost btn-sm">Xem tất cả</a>
>>>>>>> a774dae7a97873962cbcdc147ce51e50f1dd3c0e
        </div>
        <table class="tg-table">
            <thead>
                <tr><th>Điểm POI</th><th>Thời gian</th></tr>
            </thead>
            <tbody>
                @foreach (var h in Model.RecentHistory)
                {
                    <tr>
<<<<<<< HEAD
                        <td style="font-weight:500">@(h.PoiName ?? $"POI #{h.PoiId}")</td>
                        <td class="text-muted" style="font-size:13px">
                            @h.PlayTime.ToString("dd/MM HH:mm")
                        </td>
=======
                        <td>@h.PoiName</td>
                        <td class="text-muted">@h.PlayTime.ToString("dd/MM HH:mm")</td>
>>>>>>> a774dae7a97873962cbcdc147ce51e50f1dd3c0e
                    </tr>
                }
                @if (!Model.RecentHistory.Any())
                {
<<<<<<< HEAD
                    <tr>
                        <td colspan="2" class="empty-state">Chưa có lịch sử</td>
                    </tr>
                }
            </tbody>
        </table>
        @if (Model.TotalHistory > 5)
        {
            <div style="padding:.75rem 1rem;border-top:1px solid #f1f5f9">
                <a href="/HistoryPage" style="font-size:13px;color:#3b82f6;text-decoration:none">
                    Xem tất cả @Model.TotalHistory lượt phát →
                </a>
            </div>
        }
    </div>
</div>

<!-- ── Truy cập nhanh ── -->
<div class="card">
    <div class="card-head">
        <i class="bi bi-lightning-charge-fill me-2" style="color:#f59e0b"></i>
        Truy cập nhanh
    </div>
    <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:12px;padding:1rem">
        <a href="/Pois" class="quick-link">
            <i class="bi bi-geo-alt-fill" style="color:#3b82f6"></i>
            <span>Điểm thuyết minh</span>
        </a>
        <a href="/HistoryPage" class="quick-link">
            <i class="bi bi-clock-history" style="color:#8b5cf6"></i>
            <span>Lịch sử phát</span>
        </a>
        <a href="/Users" class="quick-link">
            <i class="bi bi-people-fill" style="color:#10b981"></i>
            <span>Người dùng</span>
        </a>
    </div>
</div>

<style>
    .stat-card {
        background: #fff;
        border: 1px solid #e2e8f0;
        border-radius: 10px;
        padding: 18px;
        display: flex;
        align-items: center;
        gap: 14px;
        box-shadow: 0 1px 3px rgba(0,0,0,.05);
    }

    .stat-icon {
        width: 48px;
        height: 48px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 22px;
        flex-shrink: 0;
    }

    .stat-val {
        font-size: 26px;
        font-weight: 700;
        color: #0f172a;
        line-height: 1;
    }

    .stat-label {
        font-size: 13px;
        color: #64748b;
        margin-top: 4px;
    }

    .card-head {
        padding: .875rem 1rem;
        border-bottom: 1px solid #f1f5f9;
        font-size: 14px;
        font-weight: 600;
        color: #374151;
    }

    .quick-link {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 12px 14px;
        border: 1px solid #e2e8f0;
        border-radius: 8px;
        text-decoration: none;
        color: #1e293b;
        font-size: 14px;
        font-weight: 500;
        transition: background .15s, border-color .15s;
    }

        .quick-link:hover {
            background: #f8fafc;
            border-color: #3b82f6;
        }

        .quick-link i {
            font-size: 20px;
        }
</style>
=======
                    <tr><td colspan="2" class="empty-state">Chưa có lịch sử</td></tr>
                }
            </tbody>
        </table>
    </div>

    <div class="card">
        <div class="card-header">
            <span><i class="bi bi-trophy me-2"></i>Top POI được phát nhiều nhất</span>
        </div>
        <table class="tg-table">
            <thead>
                <tr><th>Điểm</th><th>Lượt phát</th></tr>
            </thead>
            <tbody>
                @{
                    int rank = 1;
                }
                @foreach (var p in Model.TopPoi)
                {
                    <tr>
                        <td>
                            <span class="badge @(rank == 1 ? "badge-yellow" : "badge-blue")">#@rank</span>
                            <span class="ms-2">@p.Key</span>
                        </td>
                        <td><span class="badge badge-green">@p.Value lượt</span></td>
                    </tr>
                    rank++;
                }
                @if (!Model.TopPoi.Any())
                {
                    <tr><td colspan="2" class="empty-state">Chưa có dữ liệu</td></tr>
                }
            </tbody>
        </table>
    </div>
</div>
>>>>>>> a774dae7a97873962cbcdc147ce51e50f1dd3c0e
